#include<bits/stdc++.h>
using namespace std;
int main()
{
	char str[40];
	int length;
	
	cout<<"Enter a String";
	cin.getline(str,40);
	length = strlen(str);
	
	cout<<"Length of the String is "<<length<<endl;
	
	
	 return 0;
}

